RewriteEngine On
RewriteBase /appliances_api/public/

# ส่งทุกอย่างไปยัง index.php (ยกเว้นไฟล์จริง)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
